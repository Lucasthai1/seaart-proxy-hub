# seaart-proxy-hub
SeaArt Proxy Hub — API Node/Fastify que expõe chat, geração de imagem (SeaArt) e workflows (ComfyUI) por trás de um painel único, com gateway MCP, quotas freemium, Docker Compose e guia de deploy em VPS.
